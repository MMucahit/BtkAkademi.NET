@model Product

<!--
    <div class="text-center">
    <h1>@Model.Name</h1>
    <span class="bg-danger  text-white">@Model.Price.ToString("c")</span>

    <div>
        <a
            class="btn btn-primary"
            asp-controller="Product"
            asp-action="Index">Product List</a>
    </div>
</div>
-->

<div class="row ">
        <div class="card mx-auto" style="width: 18rem;">
        <img src="~/images/products/1.jpg" class="card-img-top" alt="...">

            <div class="card-body">
                <div>
                <div class="row">
                    <a asp-controller="Product" asp-action="GetOneProduct" asp-route-id=@Model.Id>@Model.Name</a>
                </div>
                <div class="row text-danger">@Model.Name</div>
                <div class="row text-danger">@Model.Description</div>
                </div>
            </div>
        </div>
</div>
